const React = require ( 'react' );
const Clock = require('Clock');
const Countdown= require('Countdown');

module.exports = () => {
    return (
        <div>
            <Clock />
        </div>
    );
};